# Internationalization (i18n) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Serve the amadis UI in French and English, with every user-facing string in per-locale catalogs, the locale auto-detected (cookie then `Accept-Language`), a language toggle, and a CI job that fails when the catalogs drift.

**Architecture:** Use next-intl without locale-in-URL routing. A `getRequestConfig` resolves the locale from the `NEXT_LOCALE` cookie, then the `Accept-Language` header, then falls back to `en`, and loads `messages/<locale>.json`. Server components read strings with `getTranslations`, client components with `useTranslations`, both under a `NextIntlClientProvider` set in the root layout. A dependency-free Python script enforces that every locale is an exact in-order mirror of `en`.

**Tech Stack:** Next.js 16.2.9 (App Router), React 19.2.7, TypeScript, next-intl 4.13, Vitest, Python 3 (stdlib only), GitHub Actions.

## Global Constraints

- next-intl version: `^4.13.0` (peer deps allow Next `^16.0.0`, React `^19.0.0`).
- Locales: `en` and `fr`. Default and final fallback: `en`.
- Locale resolution order: `NEXT_LOCALE` cookie, then `Accept-Language` header matched against `['fr','en']`, then `en`.
- No locale in the URL. Routes stay unchanged. No `[locale]` segment, no redirect middleware.
- Catalogs are nested JSON, one object per namespace, authored `en` first and mirrored by `fr` key-for-key in the same order.
- CI parity script is Python 3, standard library only, named `.github/scripts/check_translations.py`. It must run on `ubuntu-latest`'s preinstalled Python with no `setup-python` and no pip installs.
- Server-action error strings are translated at the throw site with `await getTranslations('errors')`.
- Do NOT translate: brand word `amadis`, Old French specimen text, icon glyphs, route paths, data values (tag codes, user notes, source field), CSS classes, MIME types, HTTP headers.
- Package manager is npm. Commit messages end with the repo's `Co-Authored-By` trailer.
- Never use dashes mid-sentence in prose or commit bodies. The em-dashes inside catalog string *values* are source copy and stay verbatim.

---

## File Structure

- `i18n/config.ts` — locale list, default locale, `Locale` type. Single source of truth.
- `i18n/resolve-locale.ts` — pure helper that picks the best locale from a cookie value and an `Accept-Language` header.
- `i18n/request.ts` — next-intl `getRequestConfig`, wires the helper and loads catalogs.
- `messages/en.json`, `messages/fr.json` — catalogs.
- `next.config.ts` — wrapped with `createNextIntlPlugin`.
- `app/layout.tsx` — async, `NextIntlClientProvider`, `lang`, `generateMetadata`.
- `app/lang-toggle.tsx` — client language switcher.
- `app/i18n-actions.ts` — server action that writes the `NEXT_LOCALE` cookie.
- `test-utils/intl.tsx` — `renderWithIntl` for client-component tests.
- `.github/scripts/check_translations.py` — parity check with `--self-test`.
- `.github/workflows/ci.yml` — new `i18n` job, added to the `check` gate.
- Component and action files under `app/` — strings replaced with `t(...)` calls.

---

## Task 1: Install next-intl and add locale config

**Files:**
- Modify: `package.json` (dependencies)
- Create: `i18n/config.ts`
- Create: `messages/en.json`
- Create: `messages/fr.json`
- Test: `i18n/config.test.ts`

**Interfaces:**
- Produces: `locales` (`readonly ['en','fr']`), `defaultLocale` (`'en'`), `type Locale = 'en' | 'fr'` from `@/i18n/config`.

- [ ] **Step 1: Install the dependency**

Run: `npm install next-intl@^4.13.0`
Expected: `package.json` gains `"next-intl": "^4.13.0"` under dependencies, `package-lock.json` updates, exit 0.

- [ ] **Step 2: Write the failing test**

Create `i18n/config.test.ts`:

```ts
import { describe, expect, it } from "vitest";

import { defaultLocale, locales } from "@/i18n/config";

describe("i18n config", () => {
  it("declares exactly en and fr", () => {
    expect([...locales]).toEqual(["en", "fr"]);
  });

  it("defaults to en", () => {
    expect(defaultLocale).toBe("en");
    expect(locales).toContain(defaultLocale);
  });
});
```

- [ ] **Step 3: Run test to verify it fails**

Run: `npm test -- i18n/config.test.ts`
Expected: FAIL, cannot resolve `@/i18n/config`.

- [ ] **Step 4: Create the config**

Create `i18n/config.ts`:

```ts
export const locales = ["en", "fr"] as const;

export const defaultLocale: Locale = "en";

export type Locale = (typeof locales)[number];
```

- [ ] **Step 5: Create empty catalogs**

Create `messages/en.json` with `{}` and `messages/fr.json` with `{}`. (Namespaces are filled in by later tasks so each commit keeps the catalogs in parity.)

- [ ] **Step 6: Run test to verify it passes**

Run: `npm test -- i18n/config.test.ts`
Expected: PASS (2 tests).

- [ ] **Step 7: Commit**

```bash
git add package.json package-lock.json i18n/config.ts i18n/config.test.ts messages/en.json messages/fr.json
git commit -m "feat: add next-intl and locale config (#9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: Locale resolution helper

**Files:**
- Create: `i18n/resolve-locale.ts`
- Test: `i18n/resolve-locale.test.ts`

**Interfaces:**
- Consumes: `locales`, `defaultLocale`, `Locale` from `@/i18n/config`.
- Produces: `resolveLocale(cookieValue: string | undefined, acceptLanguage: string | null): Locale` from `@/i18n/resolve-locale`.

- [ ] **Step 1: Write the failing test**

Create `i18n/resolve-locale.test.ts`:

```ts
import { describe, expect, it } from "vitest";

import { resolveLocale } from "@/i18n/resolve-locale";

describe("resolveLocale", () => {
  it("prefers a valid cookie over the header", () => {
    expect(resolveLocale("fr", "en-US,en;q=0.9")).toBe("fr");
  });

  it("ignores an unsupported cookie and falls through to the header", () => {
    expect(resolveLocale("de", "fr-FR,fr;q=0.9")).toBe("fr");
  });

  it("matches the best supported language from Accept-Language", () => {
    expect(resolveLocale(undefined, "de-DE,fr;q=0.8,en;q=0.6")).toBe("fr");
  });

  it("matches a base language ignoring the region subtag", () => {
    expect(resolveLocale(undefined, "fr-CA")).toBe("fr");
  });

  it("falls back to en when nothing matches", () => {
    expect(resolveLocale(undefined, "de-DE,es;q=0.8")).toBe("en");
  });

  it("falls back to en when there is no cookie and no header", () => {
    expect(resolveLocale(undefined, null)).toBe("en");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- i18n/resolve-locale.test.ts`
Expected: FAIL, cannot resolve `@/i18n/resolve-locale`.

- [ ] **Step 3: Write the implementation**

Create `i18n/resolve-locale.ts`:

```ts
import { defaultLocale, locales, type Locale } from "@/i18n/config";

function isLocale(value: string): value is Locale {
  return (locales as readonly string[]).includes(value);
}

// Parse an Accept-Language header into base language codes ordered by q-weight.
function rankedLanguages(header: string): string[] {
  return header
    .split(",")
    .map((part) => {
      const [tag, ...params] = part.trim().split(";");
      const q = params
        .map((p) => p.trim())
        .find((p) => p.startsWith("q="));
      const weight = q ? Number(q.slice(2)) : 1;
      return { base: tag.toLowerCase().split("-")[0], weight: Number.isNaN(weight) ? 0 : weight };
    })
    .filter((entry) => entry.base.length > 0)
    .sort((a, b) => b.weight - a.weight)
    .map((entry) => entry.base);
}

export function resolveLocale(
  cookieValue: string | undefined,
  acceptLanguage: string | null,
): Locale {
  if (cookieValue && isLocale(cookieValue)) {
    return cookieValue;
  }
  if (acceptLanguage) {
    for (const base of rankedLanguages(acceptLanguage)) {
      if (isLocale(base)) {
        return base;
      }
    }
  }
  return defaultLocale;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- i18n/resolve-locale.test.ts`
Expected: PASS (6 tests).

- [ ] **Step 5: Commit**

```bash
git add i18n/resolve-locale.ts i18n/resolve-locale.test.ts
git commit -m "feat: locale resolution from cookie and Accept-Language (#9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: Request config and plugin wiring

**Files:**
- Create: `i18n/request.ts`
- Modify: `next.config.ts`

**Interfaces:**
- Consumes: `resolveLocale` from `@/i18n/resolve-locale`.
- Produces: a default-exported `getRequestConfig` result `{ locale, messages }` consumed by next-intl at runtime. No test (it is request-context glue covered end-to-end by the build and by component tests).

- [ ] **Step 1: Create the request config**

Create `i18n/request.ts`:

```ts
import { cookies, headers } from "next/headers";
import { getRequestConfig } from "next-intl/server";

import { resolveLocale } from "@/i18n/resolve-locale";

export default getRequestConfig(async () => {
  const cookieStore = await cookies();
  const headerStore = await headers();
  const locale = resolveLocale(
    cookieStore.get("NEXT_LOCALE")?.value,
    headerStore.get("accept-language"),
  );

  return {
    locale,
    messages: (await import(`@/messages/${locale}.json`)).default,
  };
});
```

- [ ] **Step 2: Wrap next.config with the plugin**

Replace the entire contents of `next.config.ts` with:

```ts
import type { NextConfig } from "next";
import createNextIntlPlugin from "next-intl/plugin";

const withNextIntl = createNextIntlPlugin("./i18n/request.ts");

const nextConfig: NextConfig = {
  /* config options here */
};

export default withNextIntl(nextConfig);
```

- [ ] **Step 3: Verify typecheck and build wiring**

Run: `npm run typecheck`
Expected: exit 0, no type errors.

- [ ] **Step 4: Commit**

```bash
git add i18n/request.ts next.config.ts
git commit -m "feat: wire next-intl request config and plugin (#9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: Root layout provider, lang, and metadata

**Files:**
- Modify: `app/layout.tsx`
- Modify: `messages/en.json`, `messages/fr.json` (add `home.metadata`)

**Interfaces:**
- Consumes: next-intl `NextIntlClientProvider`, `getLocale`, `getTranslations`.
- Produces: a `NextIntlClientProvider` wrapping all pages so `useTranslations` works in client components; `home.metadata.title` and `home.metadata.description` keys.

- [ ] **Step 1: Add the metadata namespace to both catalogs**

Set `messages/en.json` to:

```json
{
  "home": {
    "metadata": {
      "title": "amadis — manuscript annotation",
      "description": "Annotate Old French manuscripts across linguistic layers."
    }
  }
}
```

Set `messages/fr.json` to:

```json
{
  "home": {
    "metadata": {
      "title": "amadis — annotation de manuscrits",
      "description": "Annotez des manuscrits en ancien français à travers les couches linguistiques."
    }
  }
}
```

- [ ] **Step 2: Rewrite the layout**

Replace `app/layout.tsx` with:

```tsx
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { NextIntlClientProvider } from "next-intl";
import { getLocale, getTranslations } from "next-intl/server";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export async function generateMetadata(): Promise<Metadata> {
  const t = await getTranslations("home.metadata");
  return {
    title: t("title"),
    description: t("description"),
  };
}

// Apply the saved theme before paint so there is no light/dark flash.
const themeScript = `(function(){try{var t=localStorage.getItem("amadis-theme");document.documentElement.dataset.theme=t==="light"?"light":"dark";}catch(e){document.documentElement.dataset.theme="dark";}})();`;

export default async function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  const locale = await getLocale();

  return (
    <html
      lang={locale}
      data-theme="dark"
      suppressHydrationWarning
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <script dangerouslySetInnerHTML={{ __html: themeScript }} />
        <NextIntlClientProvider>{children}</NextIntlClientProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 3: Verify typecheck passes**

Run: `npm run typecheck`
Expected: exit 0.

- [ ] **Step 4: Commit**

```bash
git add app/layout.tsx messages/en.json messages/fr.json
git commit -m "feat: provide locale and translated metadata in root layout (#9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: Client-component test helper

**Files:**
- Create: `test-utils/intl.tsx`
- Test: covered by use in later tasks (no standalone test).

**Interfaces:**
- Produces: `renderWithIntl(ui: React.ReactElement, locale?: "en" | "fr"): RenderResult` from `@/test-utils/intl`. It wraps `ui` in `NextIntlClientProvider` with the imported catalog so `useTranslations` resolves in jsdom.

- [ ] **Step 1: Create the helper**

Create `test-utils/intl.tsx`:

```tsx
import { render, type RenderResult } from "@testing-library/react";
import { NextIntlClientProvider } from "next-intl";
import type { ReactElement } from "react";

import en from "@/messages/en.json";
import fr from "@/messages/fr.json";

const catalogs = { en, fr };

export function renderWithIntl(
  ui: ReactElement,
  locale: "en" | "fr" = "en",
): RenderResult {
  return render(
    <NextIntlClientProvider locale={locale} messages={catalogs[locale]}>
      {ui}
    </NextIntlClientProvider>,
  );
}
```

- [ ] **Step 2: Verify typecheck passes**

Run: `npm run typecheck`
Expected: exit 0. (`resolveJsonModule` is already enabled by Next's tsconfig; if a JSON import errors, confirm `tsconfig.json` has `"resolveJsonModule": true`.)

- [ ] **Step 3: Commit**

```bash
git add test-utils/intl.tsx
git commit -m "test: add NextIntlClientProvider render helper (#9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: Parity check script

**Files:**
- Create: `.github/scripts/check_translations.py`
- Modify: `package.json` (scripts)

**Interfaces:**
- Produces: a CLI that exits non-zero when any `messages/<locale>.json` is not an exact in-order mirror of `messages/en.json`; `--self-test` exercises pass/missing/extra/reorder; `npm run i18n:check` invokes it.

- [ ] **Step 1: Write the script**

Create `.github/scripts/check_translations.py`:

```python
#!/usr/bin/env python3
"""Fail when any locale catalog is not an exact in-order mirror of the reference.

Reference locale is en. Every other messages/<locale>.json must contain exactly
the same nested keys, in the same declaration order. Reports missing keys, extra
keys, and the first order divergence per locale.
"""
import json
import os
import sys
from glob import glob

REFERENCE = "en"
MESSAGES_DIR = os.path.join(os.path.dirname(__file__), "..", "..", "messages")


def flatten(obj, prefix=""):
    """Return an ordered list of dot-path keys (depth-first, declaration order)."""
    keys = []
    for key, value in obj.items():
        path = f"{prefix}{key}"
        if isinstance(value, dict):
            keys.extend(flatten(value, path + "."))
        else:
            keys.append(path)
    return keys


def compare(reference_keys, locale_keys):
    """Return (missing, extra, order_divergence_index_or_None)."""
    ref_set, loc_set = set(reference_keys), set(locale_keys)
    missing = [k for k in reference_keys if k not in loc_set]
    extra = [k for k in locale_keys if k not in ref_set]
    order_at = None
    if not missing and not extra:
        for i, (a, b) in enumerate(zip(reference_keys, locale_keys)):
            if a != b:
                order_at = i
                break
    return missing, extra, order_at


def check(catalogs):
    """catalogs: dict of locale -> parsed JSON. Returns list of error lines."""
    errors = []
    reference_keys = flatten(catalogs[REFERENCE])
    for locale in sorted(catalogs):
        if locale == REFERENCE:
            continue
        missing, extra, order_at = compare(reference_keys, flatten(catalogs[locale]))
        for k in missing:
            errors.append(f"[{locale}] missing key: {k}")
        for k in extra:
            errors.append(f"[{locale}] extra key: {k}")
        if order_at is not None:
            errors.append(
                f"[{locale}] order diverges at position {order_at}: "
                f"expected {reference_keys[order_at]!r}"
            )
    return errors


def load_from_disk():
    catalogs = {}
    for path in glob(os.path.join(MESSAGES_DIR, "*.json")):
        locale = os.path.splitext(os.path.basename(path))[0]
        with open(path, encoding="utf-8") as fh:
            catalogs[locale] = json.load(fh)
    return catalogs


def self_test():
    base = {"a": {"b": 1, "c": 2}, "d": 3}
    cases = {
        "matching": ({"a": {"b": 9, "c": 9}, "d": 9}, 0),
        "missing": ({"a": {"b": 1}, "d": 3}, 1),
        "extra": ({"a": {"b": 1, "c": 2}, "d": 3, "e": 4}, 1),
        "reordered": ({"d": 3, "a": {"b": 1, "c": 2}}, 1),
    }
    failures = 0
    for name, (other, want_errors) in cases.items():
        got = check({"en": base, "fr": other})
        status = "ok" if len(got) == want_errors else "WRONG"
        if status == "WRONG":
            failures += 1
        print(f"self-test {name}: {status} ({len(got)} errors) {got}")
    if failures:
        print(f"self-test FAILED: {failures} case(s) wrong")
        return 1
    print("self-test passed")
    return 0


def main(argv):
    if "--self-test" in argv:
        return self_test()
    catalogs = load_from_disk()
    if REFERENCE not in catalogs:
        print(f"reference locale {REFERENCE!r} not found in messages/")
        return 1
    errors = check(catalogs)
    if errors:
        print("Translation catalogs are out of sync:")
        for line in errors:
            print("  " + line)
        return 1
    print(f"All {len(catalogs)} locale catalogs are in-order mirrors of {REFERENCE!r}.")
    return 0


if __name__ == "__main__":
    sys.exit(main(sys.argv[1:]))
```

- [ ] **Step 2: Run the self-test to verify it passes**

Run: `python3 .github/scripts/check_translations.py --self-test`
Expected: prints `self-test passed`, exit 0.

- [ ] **Step 3: Run against the real catalogs**

Run: `python3 .github/scripts/check_translations.py`
Expected: prints `All 2 locale catalogs are in-order mirrors of 'en'.`, exit 0 (the `home` namespace from Task 4 matches in both).

- [ ] **Step 4: Add the npm script**

In `package.json`, add to `"scripts"`:

```json
    "i18n:check": "python3 .github/scripts/check_translations.py",
```

- [ ] **Step 5: Verify the npm script**

Run: `npm run i18n:check`
Expected: same success line, exit 0.

- [ ] **Step 6: Commit**

```bash
git add .github/scripts/check_translations.py package.json
git commit -m "feat: add i18n catalog parity check script (#9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: Extract nav, theme, and app-shell strings

**Files:**
- Modify: `app/app-shell.tsx`
- Modify: `app/theme-toggle.tsx`
- Modify: `app/app-shell.test.tsx`, `app/theme-toggle.test.tsx`
- Modify: `messages/en.json`, `messages/fr.json` (add `nav`, `common`)

**Interfaces:**
- Consumes: `useTranslations` from `next-intl`, `renderWithIntl` from `@/test-utils/intl`.
- Produces: `nav.*` and `common.*` catalog keys.

- [ ] **Step 1: Add namespaces to both catalogs (in order, after `home`)**

In `messages/en.json` add `common` and `nav` objects:

```json
  "common": {
    "cancel": "Cancel",
    "delete": "Delete",
    "defaultUserName": "Scholar",
    "theme": {
      "switchToLightAria": "Switch to light theme",
      "switchToDarkAria": "Switch to dark theme",
      "switchToLightTitle": "Switch to light",
      "switchToDarkTitle": "Switch to dark"
    }
  },
  "nav": {
    "dashboard": "Dashboard",
    "textes": "Textes",
    "signIn": "Sign in",
    "signOut": "Sign out",
    "openNavigation": "Open navigation"
  }
```

In `messages/fr.json` add the same keys in the same order:

```json
  "common": {
    "cancel": "Annuler",
    "delete": "Supprimer",
    "defaultUserName": "Chercheur",
    "theme": {
      "switchToLightAria": "Passer au thème clair",
      "switchToDarkAria": "Passer au thème sombre",
      "switchToLightTitle": "Passer au clair",
      "switchToDarkTitle": "Passer au sombre"
    }
  },
  "nav": {
    "dashboard": "Tableau de bord",
    "textes": "Textes",
    "signIn": "Se connecter",
    "signOut": "Se déconnecter",
    "openNavigation": "Ouvrir la navigation"
  }
```

- [ ] **Step 2: Verify parity stays green**

Run: `npm run i18n:check`
Expected: exit 0.

- [ ] **Step 3: Update the theme-toggle test to use the provider**

In `app/theme-toggle.test.tsx`, replace each `render(<ThemeToggle />)` call with `renderWithIntl(<ThemeToggle />)` and add the import `import { renderWithIntl } from "@/test-utils/intl";`. Leave the assertions unchanged (the English strings still apply).

- [ ] **Step 4: Translate theme-toggle**

In `app/theme-toggle.tsx`, add `import { useTranslations } from "next-intl";`. Inside `ThemeToggle`, after the `useSyncExternalStore` line add `const t = useTranslations("common.theme");`. Replace the `aria-label` and `title` expressions:

```tsx
      aria-label={theme === "dark" ? t("switchToLightAria") : t("switchToDarkAria")}
      title={theme === "dark" ? t("switchToLightTitle") : t("switchToDarkTitle")}
```

- [ ] **Step 5: Update the app-shell test to use the provider**

In `app/app-shell.test.tsx`, add `import { renderWithIntl } from "@/test-utils/intl";` and replace every `render(` of `<AppShell ...>` with `renderWithIntl(`. Assertions stay (English strings).

- [ ] **Step 6: Translate app-shell**

In `app/app-shell.tsx`, add `import { useTranslations } from "next-intl";`. The `NAV` array currently bakes in `label` strings; move labels to keys. Change the `NavItem` type's `label` to `labelKey: "dashboard" | "textes"`, set `labelKey: "dashboard"` and `labelKey: "textes"` in the two entries (drop the `label` fields). Inside `AppShell`, add `const t = useTranslations();`. Render the nav label with `{t(\`nav.${item.labelKey}\`)}` in place of `{item.label}`. Replace the remaining literals:
  - `user.name ?? "Scholar"` becomes `user.name ?? t("common.defaultUserName")`
  - `Sign out` button text becomes `{t("nav.signOut")}`
  - `aria-label="Open navigation"` becomes `aria-label={t("nav.openNavigation")}`

- [ ] **Step 7: Run the affected tests**

Run: `npm test -- app/app-shell.test.tsx app/theme-toggle.test.tsx`
Expected: PASS (all existing cases green through the provider).

- [ ] **Step 8: Commit**

```bash
git add app/app-shell.tsx app/theme-toggle.tsx app/app-shell.test.tsx app/theme-toggle.test.tsx messages/en.json messages/fr.json
git commit -m "feat: translate app shell, nav and theme toggle (#9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: Extract home page strings

**Files:**
- Modify: `app/page.tsx`
- Modify: `messages/en.json`, `messages/fr.json` (add `home.hero`)

**Interfaces:**
- Consumes: `getTranslations` from `next-intl/server`; reuses `nav.*` from Task 7.
- Produces: `home.hero.*` keys.

- [ ] **Step 1: Add `home.hero` to both catalogs**

Inside the existing `home` object in `messages/en.json`, after `metadata`, add:

```json
    "hero": {
      "eyebrow": "Old French · digital philology",
      "heading": "Annotate the manuscript, layer by layer.",
      "lede": "amadis is a reading room for Old French texts. Transcribe a folio, attach its scan, then gloss any word or passage across parts of speech, lemmas, morphology and meaning — each layer its own colour, stacked beneath the line.",
      "openTextes": "Open my textes",
      "createAccount": "Create an account"
    }
```

In `messages/fr.json`, inside `home` after `metadata`:

```json
    "hero": {
      "eyebrow": "Ancien français · philologie numérique",
      "heading": "Annotez le manuscrit, couche par couche.",
      "lede": "amadis est une salle de lecture pour les textes en ancien français. Transcrivez un folio, joignez son scan, puis glosez n'importe quel mot ou passage selon les parties du discours, les lemmes, la morphologie et le sens — chaque couche a sa propre couleur, empilée sous la ligne.",
      "openTextes": "Ouvrir mes textes",
      "createAccount": "Créer un compte"
    }
```

- [ ] **Step 2: Verify parity**

Run: `npm run i18n:check`
Expected: exit 0.

- [ ] **Step 3: Translate the page**

In `app/page.tsx`, add `import { getTranslations } from "next-intl/server";`. At the top of `Home`, after the session read, add `const t = await getTranslations();`. Replace literals with calls:
  - `Dashboard` link to `{t("nav.dashboard")}`
  - both `Sign out` and `Sign in` to `{t("nav.signOut")}` / `{t("nav.signIn")}`
  - eyebrow `Old French · digital philology` to `{t("home.hero.eyebrow")}`
  - h1 to `{t("home.hero.heading")}`
  - lede paragraph body to `{t("home.hero.lede")}`
  - `Open my textes` to `{t("home.hero.openTextes")}`
  - `Create an account` to `{t("home.hero.createAccount")}`

Leave the specimen block (`Chanson de Roland`, `Carles li reis`, ...) and the brand `amadis` in the logo span unchanged.

- [ ] **Step 4: Verify typecheck**

Run: `npm run typecheck`
Expected: exit 0.

- [ ] **Step 5: Commit**

```bash
git add app/page.tsx messages/en.json messages/fr.json
git commit -m "feat: translate landing page (#9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: Extract auth pages and auth action errors

**Files:**
- Modify: `app/auth/sign-in/page.tsx`, `app/auth/sign-up/page.tsx`
- Modify: `app/auth/sign-in/actions.ts`, `app/auth/sign-up/actions.ts`
- Modify: `messages/en.json`, `messages/fr.json` (add `auth`, `errors`)
- Test: `app/auth/sign-in/page.test.tsx` (new)

**Interfaces:**
- Consumes: `useTranslations` (pages, client), `getTranslations` (actions, server), `renderWithIntl`.
- Produces: `auth.*` and `errors.signInFailed`, `errors.emailRequired`, `errors.signUpFailed`.

- [ ] **Step 1: Add `auth` and start `errors` in both catalogs**

In `messages/en.json` add after `nav`:

```json
  "auth": {
    "signIn": {
      "title": "Welcome back",
      "submit": "Sign in",
      "submitPending": "Signing in…",
      "noAccountPrompt": "No account yet?",
      "createOneLink": "Create one"
    },
    "signUp": {
      "title": "Create your account",
      "submit": "Create account",
      "submitPending": "Creating account…",
      "haveAccountPrompt": "Already have an account?",
      "signInLink": "Sign in"
    },
    "fields": {
      "name": "Name",
      "email": "Email",
      "password": "Password"
    }
  },
  "errors": {
    "signInFailed": "Failed to sign in. Try again.",
    "emailRequired": "Email address must be provided.",
    "signUpFailed": "Failed to create account"
  }
```

In `messages/fr.json` add the same structure in the same order:

```json
  "auth": {
    "signIn": {
      "title": "Bon retour",
      "submit": "Se connecter",
      "submitPending": "Connexion…",
      "noAccountPrompt": "Pas encore de compte ?",
      "createOneLink": "Créez-en un"
    },
    "signUp": {
      "title": "Créez votre compte",
      "submit": "Créer un compte",
      "submitPending": "Création du compte…",
      "haveAccountPrompt": "Vous avez déjà un compte ?",
      "signInLink": "Se connecter"
    },
    "fields": {
      "name": "Nom",
      "email": "E-mail",
      "password": "Mot de passe"
    }
  },
  "errors": {
    "signInFailed": "Échec de la connexion. Réessayez.",
    "emailRequired": "Une adresse e-mail doit être fournie.",
    "signUpFailed": "Échec de la création du compte"
  }
```

- [ ] **Step 2: Verify parity**

Run: `npm run i18n:check`
Expected: exit 0.

- [ ] **Step 3: Write the failing test for the sign-in page**

Create `app/auth/sign-in/page.test.tsx`:

```tsx
import { screen } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";

import { renderWithIntl } from "@/test-utils/intl";

vi.mock("./actions", () => ({ signInWithEmail: vi.fn() }));

import SignInPage from "./page";

describe("SignInPage", () => {
  it("renders English copy by default", () => {
    renderWithIntl(<SignInPage />);
    expect(screen.getByRole("heading", { name: "Welcome back" })).toBeInTheDocument();
    expect(screen.getByRole("button", { name: "Sign in" })).toBeInTheDocument();
  });

  it("renders French copy under the fr locale", () => {
    renderWithIntl(<SignInPage />, "fr");
    expect(screen.getByRole("heading", { name: "Bon retour" })).toBeInTheDocument();
    expect(screen.getByRole("button", { name: "Se connecter" })).toBeInTheDocument();
  });
});
```

- [ ] **Step 4: Run test to verify it fails**

Run: `npm test -- app/auth/sign-in/page.test.tsx`
Expected: FAIL, the page still renders hardcoded "Welcome back" with no provider awareness (the fr case fails).

- [ ] **Step 5: Translate sign-in page**

In `app/auth/sign-in/page.tsx`, add `import { useTranslations } from "next-intl";`. After the `useActionState` line add `const t = useTranslations("auth");`. Replace:
  - `Welcome back` to `{t("signIn.title")}`
  - `Email` to `{t("fields.email")}`, `Password` to `{t("fields.password")}`
  - `{isPending ? "Signing in…" : "Sign in"}` to `{isPending ? t("signIn.submitPending") : t("signIn.submit")}`
  - `No account yet?` to `{t("signIn.noAccountPrompt")}`
  - `Create one` to `{t("signIn.createOneLink")}`

- [ ] **Step 6: Run test to verify it passes**

Run: `npm test -- app/auth/sign-in/page.test.tsx`
Expected: PASS (2 tests).

- [ ] **Step 7: Translate sign-up page**

In `app/auth/sign-up/page.tsx`, add the same `useTranslations("auth")` and replace, by reference to its current literals: title `Create your account` to `{t("signUp.title")}`, `Name` to `{t("fields.name")}`, `Email` to `{t("fields.email")}`, `Password` to `{t("fields.password")}`, the submit `{isPending ? "Creating account…" : "Create account"}` to `{isPending ? t("signUp.submitPending") : t("signUp.submit")}`, `Already have an account?` to `{t("signUp.haveAccountPrompt")}`, and the `Sign in` link to `{t("signUp.signInLink")}`.

- [ ] **Step 8: Translate the auth action error strings**

In `app/auth/sign-in/actions.ts`, add `import { getTranslations } from "next-intl/server";`. Where it currently produces the literal `"Failed to sign in. Try again."`, first obtain `const t = await getTranslations("errors");` in that scope and use `t("signInFailed")`.

In `app/auth/sign-up/actions.ts`, add the same import. Replace `"Email address must be provided."` with `t("emailRequired")` and `"Failed to create account"` with `t("signUpFailed")`, obtaining `const t = await getTranslations("errors");` before first use.

- [ ] **Step 9: Verify typecheck and full suite**

Run: `npm run typecheck && npm test`
Expected: exit 0, all tests pass.

- [ ] **Step 10: Commit**

```bash
git add app/auth messages/en.json messages/fr.json
git commit -m "feat: translate auth pages and action errors (#9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 10: Extract textes list and dashboard

**Files:**
- Modify: `app/(app)/textes/page.tsx`, `app/(app)/textes/actions.ts`
- Modify: `app/(app)/dashboard/page.tsx`
- Modify: `messages/en.json`, `messages/fr.json` (add `textes`, `dashboard`; extend `errors`)

**Interfaces:**
- Consumes: `getTranslations` from `next-intl/server`.
- Produces: `textes.*`, `dashboard.*`, `errors.referenceRequired`. Plural messages use ICU syntax.

- [ ] **Step 1: Add `textes` and `dashboard`, extend `errors`, in both catalogs**

In `messages/en.json` add after `auth` (and before `errors`):

```json
  "textes": {
    "title": "Textes",
    "corpusCount": "{count, plural, =0 {None in your corpus} one {# in your corpus} other {# in your corpus}}",
    "newTexte": "New texte",
    "myTextes": "My textes",
    "empty": "No textes yet. Create one above to begin.",
    "form": {
      "referencePlaceholder": "Reference (e.g. Roland, ms. Oxford f.1r)",
      "sourcePlaceholder": "Source (optional)",
      "contentPlaceholder": "Paste a transcription (optional — you can scan first and transcribe later)",
      "submit": "Create texte"
    },
    "list": {
      "annotationCount": "{count, plural, one {# annotation} other {# annotations}}",
      "hasScan": " · has scan"
    }
  },
  "dashboard": {
    "title": "Dashboard",
    "signedInAs": "Signed in as {name}",
    "openMyTextes": "Open my textes",
    "scholarCount": "{count, plural, one {# scholar on amadis · your work is private to you.} other {# scholars on amadis · your work is private to you.}}",
    "stats": {
      "textes": "textes",
      "tags": "tags",
      "annotations": "annotations"
    }
  },
```

Extend the existing `errors` object in `messages/en.json` with (keep prior keys, add at the end):

```json
    "referenceRequired": "Reference is required"
```

In `messages/fr.json` add after `auth`:

```json
  "textes": {
    "title": "Textes",
    "corpusCount": "{count, plural, =0 {Aucun dans votre corpus} one {# dans votre corpus} other {# dans votre corpus}}",
    "newTexte": "Nouveau texte",
    "myTextes": "Mes textes",
    "empty": "Aucun texte pour l'instant. Créez-en un ci-dessus pour commencer.",
    "form": {
      "referencePlaceholder": "Référence (par ex. Roland, ms. Oxford f.1r)",
      "sourcePlaceholder": "Source (facultatif)",
      "contentPlaceholder": "Collez une transcription (facultatif — vous pouvez scanner d'abord et transcrire plus tard)",
      "submit": "Créer le texte"
    },
    "list": {
      "annotationCount": "{count, plural, one {# annotation} other {# annotations}}",
      "hasScan": " · scan disponible"
    }
  },
  "dashboard": {
    "title": "Tableau de bord",
    "signedInAs": "Connecté en tant que {name}",
    "openMyTextes": "Ouvrir mes textes",
    "scholarCount": "{count, plural, one {# chercheur sur amadis · votre travail vous est privé.} other {# chercheurs sur amadis · votre travail vous est privé.}}",
    "stats": {
      "textes": "textes",
      "tags": "étiquettes",
      "annotations": "annotations"
    }
  },
```

Extend `errors` in `messages/fr.json` end with:

```json
    "referenceRequired": "La référence est requise"
```

- [ ] **Step 2: Verify parity**

Run: `npm run i18n:check`
Expected: exit 0.

- [ ] **Step 3: Translate the textes list page**

In `app/(app)/textes/page.tsx`, add `import { getTranslations } from "next-intl/server";` and `const t = await getTranslations("textes");` near the top of the component. Replace:
  - `Textes` heading to `{t("title")}`
  - the `{count} in your corpus` text to `{t("corpusCount", { count })}` (use the existing count variable feeding that line)
  - `New texte` to `{t("newTexte")}`, `My textes` to `{t("myTextes")}`
  - the three form placeholders to `t("form.referencePlaceholder")`, `t("form.sourcePlaceholder")`, `t("form.contentPlaceholder")`
  - `Create texte` to `{t("form.submit")}`
  - the annotation count (currently `n + " annotation" + (n === 1 ? "" : "s")`) to `{t("list.annotationCount", { count: n })}` where `n` is that annotation count
  - ` · has scan` to `{t("list.hasScan")}`
  - the empty-state to `{t("empty")}`
  - the per-row `Delete` button to use the common namespace: add `const tc = await getTranslations("common");` and render `{tc("delete")}`.

- [ ] **Step 4: Translate the textes action error**

In `app/(app)/textes/actions.ts`, add `import { getTranslations } from "next-intl/server";`. Before the `Reference is required` throw, add `const t = await getTranslations("errors");` and use `t("referenceRequired")`.

- [ ] **Step 4b: Mock the server translator in the action test**

`app/(app)/textes/actions.test.ts` asserts the thrown message, so `getTranslations` must resolve to the real `en` catalog in jsdom. Add this `vi.mock` to the head of that test file (after the existing imports/mocks):

```ts
vi.mock("next-intl/server", async () => {
  const en = (await import("@/messages/en.json")).default as Record<string, unknown>;
  const get = (obj: unknown, path: string): unknown =>
    path.split(".").reduce<unknown>((o, k) => (o as Record<string, unknown>)?.[k], obj);
  return {
    getTranslations: async (ns: string) => (key: string) => get(en, `${ns}.${key}`) as string,
  };
});
```

This returns the literal English string for any `t("key")`, so the existing `toThrow("Reference is required")` assertion stays valid.

- [ ] **Step 5: Translate the dashboard page**

In `app/(app)/dashboard/page.tsx`, add `import { getTranslations } from "next-intl/server";` and `const t = await getTranslations("dashboard");`. Change the `mine` array to keep stable identifiers and translate at render: keep entries as `["textes", count]` etc. (the string is both React key and a `stats` subkey). Replace:
  - `<h1>Dashboard</h1>` to `<h1>{t("title")}</h1>`
  - `Signed in as {session.user.name}` to `{t("signedInAs", { name: session.user.name })}`
  - `Open my textes` to `{t("openMyTextes")}`
  - the stat label `{name}` to `{t(\`stats.${name}\`)}`
  - the whole `scholar/scholars ... private to you.` paragraph to `{t("scholarCount", { count: authUsers[0]?.count ?? 0 })}`

- [ ] **Step 6: Verify typecheck and suite**

Run: `npm run typecheck && npm test`
Expected: exit 0, all green.

- [ ] **Step 7: Commit**

```bash
git add "app/(app)/textes/page.tsx" "app/(app)/textes/actions.ts" "app/(app)/dashboard/page.tsx" messages/en.json messages/fr.json
git commit -m "feat: translate textes list and dashboard (#9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 11: Extract texte detail page and its action errors

**Files:**
- Modify: `app/(app)/textes/[id]/page.tsx`, `app/(app)/textes/[id]/actions.ts`
- Modify: `messages/en.json`, `messages/fr.json` (add `texteDetail`; extend `errors`)

**Interfaces:**
- Consumes: `getTranslations` from `next-intl/server`.
- Produces: `texteDetail.*` and `errors.texteNotFound`, `errors.tagLayerCodeRequired`, `errors.invalidSpan`, `errors.tagNotFound`, `errors.invalidScanKey`.

- [ ] **Step 1: Add `texteDetail` and extend `errors` in both catalogs**

In `messages/en.json` add after `dashboard`:

```json
  "texteDetail": {
    "allTextes": "All textes",
    "scan": {
      "label": "Scan",
      "alt": "Scan of {reference}",
      "empty": "No scan uploaded yet."
    },
    "tags": {
      "label": "Tags",
      "layerPlaceholder": "layer (e.g. pos)",
      "codePlaceholder": "code (e.g. NOUN)",
      "labelPlaceholder": "label (optional)",
      "addButton": "Add tag",
      "empty": "No tags yet. Create one to start annotating."
    },
    "tallies": {
      "perTag": "Per tag",
      "perLayer": "Per layer",
      "noAnnotations": "No annotations."
    }
  },
```

Extend `errors` in `messages/en.json` (append, keeping `referenceRequired`):

```json
    "texteNotFound": "Texte not found",
    "tagLayerCodeRequired": "Layer and code are required",
    "invalidSpan": "Invalid span",
    "tagNotFound": "Tag not found",
    "invalidScanKey": "Invalid scan key"
```

In `messages/fr.json` add after `dashboard`:

```json
  "texteDetail": {
    "allTextes": "Tous les textes",
    "scan": {
      "label": "Scan",
      "alt": "Scan de {reference}",
      "empty": "Aucun scan téléversé pour l'instant."
    },
    "tags": {
      "label": "Étiquettes",
      "layerPlaceholder": "couche (par ex. pos)",
      "codePlaceholder": "code (par ex. NOUN)",
      "labelPlaceholder": "libellé (facultatif)",
      "addButton": "Ajouter une étiquette",
      "empty": "Aucune étiquette pour l'instant. Créez-en une pour commencer à annoter."
    },
    "tallies": {
      "perTag": "Par étiquette",
      "perLayer": "Par couche",
      "noAnnotations": "Aucune annotation."
    }
  },
```

Extend `errors` in `messages/fr.json`:

```json
    "texteNotFound": "Texte introuvable",
    "tagLayerCodeRequired": "La couche et le code sont requis",
    "invalidSpan": "Plage invalide",
    "tagNotFound": "Étiquette introuvable",
    "invalidScanKey": "Clé de scan invalide"
```

- [ ] **Step 2: Verify parity**

Run: `npm run i18n:check`
Expected: exit 0.

- [ ] **Step 3: Translate the detail page**

In `app/(app)/textes/[id]/page.tsx`, add `import { getTranslations } from "next-intl/server";` and `const t = await getTranslations("texteDetail");`. Replace:
  - `All textes` to `{t("allTextes")}`
  - `Scan` section label to `{t("scan.label")}`
  - the img `alt` (currently `Scan of {texte.reference}`) to `t("scan.alt", { reference: texte.reference })`
  - `No scan uploaded yet.` to `{t("scan.empty")}`
  - `Tags` to `{t("tags.label")}`
  - the three tag form placeholders to `t("tags.layerPlaceholder")`, `t("tags.codePlaceholder")`, `t("tags.labelPlaceholder")`
  - `Add tag` to `{t("tags.addButton")}`
  - `No tags yet. Create one to start annotating.` to `{t("tags.empty")}`
  - `Per tag` to `{t("tallies.perTag")}`, `Per layer` to `{t("tallies.perLayer")}`
  - both `No annotations.` cells to `{t("tallies.noAnnotations")}`

Leave `{tag.layer}:{tag.code}` and all numeric/data cells unchanged.

- [ ] **Step 4: Translate the action errors**

In `app/(app)/textes/[id]/actions.ts`, add `import { getTranslations } from "next-intl/server";`. In each function that throws, obtain `const t = await getTranslations("errors");` before the throw and replace the literals: `"Texte not found"` to `t("texteNotFound")`, `"Layer and code are required"` to `t("tagLayerCodeRequired")`, `"Invalid span"` to `t("invalidSpan")`, `"Tag not found"` to `t("tagNotFound")`, `"Invalid scan key"` to `t("invalidScanKey")`.

- [ ] **Step 4b: Mock the server translator in the action test**

`app/(app)/textes/[id]/actions.test.ts` asserts thrown messages, so add this `vi.mock` to the head of that test file (after the existing imports/mocks):

```ts
vi.mock("next-intl/server", async () => {
  const en = (await import("@/messages/en.json")).default as Record<string, unknown>;
  const get = (obj: unknown, path: string): unknown =>
    path.split(".").reduce<unknown>((o, k) => (o as Record<string, unknown>)?.[k], obj);
  return {
    getTranslations: async (ns: string) => (key: string) => get(en, `${ns}.${key}`) as string,
  };
});
```

The existing `toThrow("Texte not found")` style assertions stay valid because the mock returns the literal English strings.

- [ ] **Step 5: Verify typecheck and suite**

Run: `npm run typecheck && npm test`
Expected: exit 0, all green.

- [ ] **Step 6: Commit**

```bash
git add "app/(app)/textes/[id]/page.tsx" "app/(app)/textes/[id]/actions.ts" messages/en.json messages/fr.json
git commit -m "feat: translate texte detail and its action errors (#9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 12: Extract annotator strings

**Files:**
- Modify: `app/(app)/textes/[id]/annotator.tsx`
- Modify: `app/(app)/textes/[id]/annotator.test.tsx`, `app/(app)/textes/[id]/annotator-interactions.test.tsx`
- Modify: `messages/en.json`, `messages/fr.json` (add `annotator`)

**Interfaces:**
- Consumes: `useTranslations` (client), `renderWithIntl`. Reuses `common.cancel`.
- Produces: `annotator.*` keys. `folio.eyebrow` is rich (`<b>`), `foot.codePoints` and `inspector.title` use ICU.

- [ ] **Step 1: Add `annotator` to both catalogs**

In `messages/en.json` add after `texteDetail`:

```json
  "annotator": {
    "palette": {
      "title": "Layers",
      "empty": "None yet — create a tag below."
    },
    "folio": {
      "eyebrow": "<b>Interlinear</b> · select a word or passage to gloss it · hover to trace",
      "noContent": "No transcription yet. Add content to this texte to annotate it."
    },
    "foot": {
      "convention": "NFC · code-point offsets · [start, end)",
      "codePoints": "{count, plural, one {# code point} other {# code points}}"
    },
    "toolbar": {
      "ariaLabel": "Inscribe a gloss",
      "noTagHint": "Create a tag first to gloss.",
      "tagAriaLabel": "Tag",
      "noTagOption": "no tag",
      "notePlaceholder": "note (optional)",
      "inscribe": "Inscribe"
    },
    "inspector": {
      "title": "Apparatus · {count}",
      "empty": "No glosses yet. Select text and pick a colour — each annotation lands on its own lane below the line.",
      "removeGloss": "Remove gloss"
    }
  },
```

In `messages/fr.json` add after `texteDetail`:

```json
  "annotator": {
    "palette": {
      "title": "Couches",
      "empty": "Aucune pour l'instant — créez une étiquette ci-dessous."
    },
    "folio": {
      "eyebrow": "<b>Interlinéaire</b> · sélectionnez un mot ou un passage pour le gloser · survolez pour tracer",
      "noContent": "Aucune transcription pour l'instant. Ajoutez du contenu à ce texte pour l'annoter."
    },
    "foot": {
      "convention": "NFC · décalages en points de code · [début, fin)",
      "codePoints": "{count, plural, one {# point de code} other {# points de code}}"
    },
    "toolbar": {
      "ariaLabel": "Inscrire une glose",
      "noTagHint": "Créez d'abord une étiquette pour gloser.",
      "tagAriaLabel": "Étiquette",
      "noTagOption": "aucune étiquette",
      "notePlaceholder": "note (facultatif)",
      "inscribe": "Inscrire"
    },
    "inspector": {
      "title": "Apparat · {count}",
      "empty": "Aucune glose pour l'instant. Sélectionnez du texte et choisissez une couleur — chaque annotation occupe sa propre voie sous la ligne.",
      "removeGloss": "Supprimer la glose"
    }
  },
```

- [ ] **Step 2: Verify parity**

Run: `npm run i18n:check`
Expected: exit 0.

- [ ] **Step 3: Update the annotator tests to use the provider**

In both `annotator.test.tsx` and `annotator-interactions.test.tsx`, add `import { renderWithIntl } from "@/test-utils/intl";` and replace each `render(<Annotator .../>)` (including the `const { container } = render(...)` forms) with the `renderWithIntl` equivalent (`const { container } = renderWithIntl(...)`). The assertions for `Apparatus · 1`, `No transcription yet`, `[1, 2)`, the emoji, and `hi` stay valid because the default locale is `en`. The inspector header assertion `screen.getByText(/Apparatus · 1/)` still matches the ICU output for `count: 1`.

- [ ] **Step 4: Translate the annotator**

In `app/(app)/textes/[id]/annotator.tsx`, add `import { useTranslations } from "next-intl";`. Inside the component add `const t = useTranslations("annotator");` and `const tc = useTranslations("common");`. Replace:
  - `Layers` to `{t("palette.title")}`
  - `None yet — create a tag below.` to `{t("palette.empty")}`
  - the eyebrow node: render rich text so the bold stays translatable:
    ```tsx
    {t.rich("folio.eyebrow", { b: (chunks) => <b>{chunks}</b> })}
    ```
    Replace the existing `<b>Interlinear</b> · ...` JSX with this single call.
  - `No transcription yet. ...` to `{t("folio.noContent")}`
  - `NFC · code-point offsets · [start, end)` to `{t("foot.convention")}`
  - the `{cps.length} code points` text to `{t("foot.codePoints", { count: cps.length })}`
  - toolbar `aria-label="Inscribe a gloss"` to `aria-label={t("toolbar.ariaLabel")}`
  - `Create a tag first to gloss.` to `{t("toolbar.noTagHint")}`
  - tag select `aria-label="Tag"` to `aria-label={t("toolbar.tagAriaLabel")}`
  - `no tag` option to `{t("toolbar.noTagOption")}`
  - note `placeholder="note (optional)"` to `placeholder={t("toolbar.notePlaceholder")}`
  - `Inscribe` button to `{t("toolbar.inscribe")}`
  - `Cancel` button to `{tc("cancel")}`
  - the inspector header `Apparatus · {annotations.length}` to `{t("inspector.title", { count: annotations.length })}`
  - `No glosses yet. ...` to `{t("inspector.empty")}`
  - `aria-label="Remove gloss"` to `aria-label={t("inspector.removeGloss")}`

Leave the per-chip `[{a.start}, {a.end})`, `{a.code}`, `{a.layer}`, `{a.note}`, line numbers, and the `×` glyph unchanged.

- [ ] **Step 5: Run the annotator tests**

Run: `npm test -- "app/(app)/textes/[id]/annotator.test.tsx" "app/(app)/textes/[id]/annotator-interactions.test.tsx"`
Expected: PASS (all existing cases green).

- [ ] **Step 6: Commit**

```bash
git add "app/(app)/textes/[id]/annotator.tsx" "app/(app)/textes/[id]/annotator.test.tsx" "app/(app)/textes/[id]/annotator-interactions.test.tsx" messages/en.json messages/fr.json
git commit -m "feat: translate the annotator (#9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 13: Extract scan-uploader strings

**Files:**
- Modify: `app/(app)/textes/[id]/scan-uploader.tsx`
- Modify: `app/(app)/textes/[id]/scan-uploader.test.tsx`
- Modify: `messages/en.json`, `messages/fr.json` (add `scanUploader`)

**Interfaces:**
- Consumes: `useTranslations` (client), `renderWithIntl`.
- Produces: `scanUploader.*` keys.

- [ ] **Step 1: Add `scanUploader` to both catalogs**

In `messages/en.json` add after `annotator`:

```json
  "scanUploader": {
    "uploadScan": "Upload scan",
    "uploading": "Uploading…",
    "errors": {
      "uploadFailed": "Upload failed",
      "uploadFailedStatus": "Upload failed ({status})"
    }
  },
```

In `messages/fr.json` add after `annotator`:

```json
  "scanUploader": {
    "uploadScan": "Téléverser un scan",
    "uploading": "Téléversement…",
    "errors": {
      "uploadFailed": "Échec du téléversement",
      "uploadFailedStatus": "Échec du téléversement ({status})"
    }
  },
```

- [ ] **Step 2: Verify parity**

Run: `npm run i18n:check`
Expected: exit 0.

- [ ] **Step 3: Update the scan-uploader test to use the provider**

In `app/(app)/textes/[id]/scan-uploader.test.tsx`, add `import { renderWithIntl } from "@/test-utils/intl";` and replace each `render(<ScanUploader .../>)` with `renderWithIntl(...)`. Existing assertions on `Upload scan` / `Uploading…` / failure text stay valid under `en`.

- [ ] **Step 4: Translate the scan uploader**

In `app/(app)/textes/[id]/scan-uploader.tsx`, add `import { useTranslations } from "next-intl";` and `const t = useTranslations("scanUploader");`. Replace:
  - the idle/pending button text `Upload scan` / `Uploading…` to `t("uploadScan")` / `t("uploading")`
  - the thrown `Upload failed (${res.status})` to `t("errors.uploadFailedStatus", { status: res.status })`
  - the fallback `Upload failed` to `t("errors.uploadFailed")`

Leave `application/octet-stream`, the `accept` attribute, and HTTP method/header literals unchanged.

- [ ] **Step 5: Run the test**

Run: `npm test -- "app/(app)/textes/[id]/scan-uploader.test.tsx"`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add "app/(app)/textes/[id]/scan-uploader.tsx" "app/(app)/textes/[id]/scan-uploader.test.tsx" messages/en.json messages/fr.json
git commit -m "feat: translate the scan uploader (#9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 14: Language toggle

**Files:**
- Create: `app/i18n-actions.ts`
- Create: `app/lang-toggle.tsx`
- Create: `app/lang-toggle.test.tsx`
- Modify: `app/app-shell.tsx` (place the toggle beside `ThemeToggle`)
- Modify: `app/page.tsx` (place the toggle in the top bar actions)

**Interfaces:**
- Consumes: `useLocale` from `next-intl`, `useTransition` from React, `setLocale` server action.
- Produces: `setLocale(locale: "en" | "fr"): Promise<void>` from `@/app/i18n-actions` that writes the `NEXT_LOCALE` cookie; `LangToggle` client component.

- [ ] **Step 1: Write the failing toggle test**

Create `app/lang-toggle.test.tsx`:

```tsx
import { fireEvent, screen } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";

import { renderWithIntl } from "@/test-utils/intl";

const setLocale = vi.fn();
vi.mock("@/app/i18n-actions", () => ({ setLocale: (l: string) => setLocale(l) }));
vi.mock("next/navigation", () => ({ useRouter: () => ({ refresh: vi.fn() }) }));

import { LangToggle } from "./lang-toggle";

describe("LangToggle", () => {
  it("shows the other language and switches to it on click", () => {
    renderWithIntl(<LangToggle />, "en");
    const button = screen.getByRole("button", { name: /Français|FR/ });
    fireEvent.click(button);
    expect(setLocale).toHaveBeenCalledWith("fr");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- app/lang-toggle.test.tsx`
Expected: FAIL, cannot resolve `./lang-toggle`.

- [ ] **Step 3: Write the server action**

Create `app/i18n-actions.ts`:

```ts
"use server";

import { cookies } from "next/headers";

import { locales, type Locale } from "@/i18n/config";

export async function setLocale(locale: Locale): Promise<void> {
  if (!(locales as readonly string[]).includes(locale)) {
    return;
  }
  const cookieStore = await cookies();
  // One year, root path, so every route resolves the chosen locale.
  cookieStore.set("NEXT_LOCALE", locale, { path: "/", maxAge: 60 * 60 * 24 * 365 });
}
```

- [ ] **Step 4: Write the toggle component**

Create `app/lang-toggle.tsx`:

```tsx
"use client";

import { useLocale } from "next-intl";
import { useRouter } from "next/navigation";
import { useTransition } from "react";

import { setLocale } from "@/app/i18n-actions";
import type { Locale } from "@/i18n/config";

const LABEL: Record<Locale, string> = { en: "EN", fr: "FR" };
const ARIA: Record<Locale, string> = {
  en: "Passer en français",
  fr: "Switch to English",
};

export function LangToggle() {
  const locale = useLocale() as Locale;
  const router = useRouter();
  const [isPending, startTransition] = useTransition();
  const next: Locale = locale === "en" ? "fr" : "en";

  function switchTo() {
    startTransition(async () => {
      await setLocale(next);
      router.refresh();
    });
  }

  return (
    <button
      type="button"
      onClick={switchTo}
      disabled={isPending}
      className="ghost icon"
      aria-label={ARIA[locale]}
      title={ARIA[locale]}
    >
      {LABEL[next]}
    </button>
  );
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `npm test -- app/lang-toggle.test.tsx`
Expected: PASS.

- [ ] **Step 6: Place the toggle in the app shell**

In `app/app-shell.tsx`, add `import { LangToggle } from "@/app/lang-toggle";`. In the sidebar `foot-actions` row, render `<LangToggle />` next to `<ThemeToggle />`. In the mobile bar, render `<LangToggle />` next to the existing `<ThemeToggle />`.

- [ ] **Step 7: Place the toggle on the landing page**

In `app/page.tsx`, add `import { LangToggle } from "@/app/lang-toggle";`. In the topbar `actions` div, render `<LangToggle />` next to `<ThemeToggle />`.

- [ ] **Step 8: Verify typecheck and full suite**

Run: `npm run typecheck && npm test`
Expected: exit 0, all tests pass. (The app-shell test from Task 7 already renders through `renderWithIntl`, so `useLocale` resolves.)

- [ ] **Step 9: Commit**

```bash
git add app/i18n-actions.ts app/lang-toggle.tsx app/lang-toggle.test.tsx app/app-shell.tsx app/page.tsx
git commit -m "feat: add language toggle (#9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 15: Wire the CI parity job

**Files:**
- Modify: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: the `check_translations.py` script from Task 6.
- Produces: an `i18n` job added to the `check` gate's `needs`.

- [ ] **Step 1: Add the `i18n` job**

In `.github/workflows/ci.yml`, after the `test` job block (and before `publish-test-results`), add a new job:

```yaml
  # Fast, dependency-free guard: the two locale catalogs must stay exact
  # in-order mirrors. Runs on the runner's preinstalled Python, no npm install.
  i18n:
    name: i18n
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: python3 .github/scripts/check_translations.py
```

- [ ] **Step 2: Add `i18n` to the aggregate gate**

In the `check` job, change its `needs` line to include `i18n`:

```yaml
    needs: [lint, typecheck, test, sonar, i18n]
```

- [ ] **Step 3: Validate the workflow locally**

Run: `python3 .github/scripts/check_translations.py && echo "workflow command ok"`
Expected: success line plus `workflow command ok`. (Optionally lint the YAML with `npx --yes yaml-lint .github/workflows/ci.yml` if available; otherwise visually confirm indentation matches the surrounding jobs.)

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: enforce i18n catalog parity (#9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 16: Full verification

**Files:** none (verification only).

- [ ] **Step 1: Run the whole suite**

Run: `npm test`
Expected: all suites pass, exit 0.

- [ ] **Step 2: Typecheck and lint**

Run: `npm run typecheck && npm run lint`
Expected: exit 0 for both.

- [ ] **Step 3: Parity check**

Run: `npm run i18n:check && python3 .github/scripts/check_translations.py --self-test`
Expected: both exit 0.

- [ ] **Step 4: Production build**

Run: `npm run build`
Expected: build succeeds. (Catches any next-intl request-config or layout wiring problem that unit tests do not exercise.)

- [ ] **Step 5: Manual locale smoke (optional, if a dev server is available)**

Run: `npm run dev`, then load the app with `Accept-Language: fr` (e.g. browser set to French) and confirm French copy renders. Click the language toggle and confirm it flips and persists across a reload. Clear the `NEXT_LOCALE` cookie and confirm the browser language is auto-detected.

---

## Notes on out-of-scope items

Annotation offset logic stays language-agnostic. No locale-in-URL routing. Stored user data and the Old French specimen content are never translated. The theme toggle keeps using `localStorage`; only the locale uses a cookie because it must be resolved server-side.
